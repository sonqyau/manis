name: CI

on:
  push:
    branches:
      - main
      - "release/*"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-26
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install SwiftFormat and SwiftLint
        run: |
          brew install swiftformat swiftlint

      - name: Linting
        run: swiftlint --fix --format && swiftlint --strict
        continue-on-error: true

      - name: Formatting
        run: swiftformat . && swiftformat . --lint
        continue-on-error: true

      - name: Show Swift version
        run: swift --version

      - name: Setup Go toolchain
        uses: actions/setup-go@v6
        with:
          go-version: "1.25"

      - name: Build (Debug)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x build.sh
          ./build.sh --mode spm --config debug --kernel
        continue-on-error: true

      - name: Build (Release)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x build.sh
          ./build.sh --mode spm --config release --kernel
        continue-on-error: true

      - name: Run tests
        run: swift test --parallel
        continue-on-error: true

  package:
    runs-on: macos-26
    needs: build
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Go toolchain
        uses: actions/setup-go@v6
        with:
          go-version: "1.25"

      - name: Generate application bundle
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          chmod +x build.sh
          ./build.sh --mode spm --config release --bundle --kernel --geo

      - name: Upload miho.app bundle
        uses: actions/upload-artifact@v5
        with:
          name: miho-app
          path: .build/release/miho.app
          if-no-files-found: error
